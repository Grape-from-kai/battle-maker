<!DOCTYPE html>
<html>
  <head>
    <title>BattleMaker</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- ナビゲーションバー -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="#"><!-- <#%  link_to "TOP"~~に置き換える %>-->TOP</a>
      <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="ナビゲーションの切替">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li><%= link_to "index",battles_path,class: "nav-link" %></li>
          <li><%= link_to "new",new_battle_path,class: "nav-link" %></li>
          <li><%= link_to "sign out",destroy_user_session_path,method: :delete,class: "nav-link" %></li>
          <li><%= link_to "profile", user_path(current_user.id),class: "nav-link" %></li>
        <% else %>
          <li><%= link_to "sign in",new_user_session_path,class: "nav-link" %></li>
          <li><%= link_to "sign up",new_user_registration_path,class: "nav-link" %></li>
        <% end %>
      </ul>
      </div>
    </nav>
    
    <!-- 制作用（あとでけす）-->
    <% if current_user.try(:admin?) %>
      <p>admin_user</p>
    <% end %>
    
    
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    
    <%= yield %>
  </body>
</html>
